import React from "react";
import styles from "./style.module.css";

const Nav = () => {
  

  return (
    <nav>
    <h1>Chatter</h1>
      {/* <a className={styles.links} href="#">Log out</a> */}
    </nav>
  );
};

export default Nav;
